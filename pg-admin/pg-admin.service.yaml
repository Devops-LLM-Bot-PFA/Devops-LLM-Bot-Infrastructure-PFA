apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: default
  labels:
    app.kubernetes.io/managed-by: admin
    app: pgadmin

spec:
  type: NodePort
  ports:
    - port: 80
  selector:
    app: pgadmin
