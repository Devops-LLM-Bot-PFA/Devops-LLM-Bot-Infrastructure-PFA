apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: default
  labels:
    app.kubernetes.io/managed-by: admin
    app: backend

spec:
  type: NodePort
  ports:
    - port: 7001
      targetPort: 7001
      nodePort: 30001
  selector:
    app: backend
